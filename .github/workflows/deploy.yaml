name: Deploy New Nginx Instance

on:
  workflow_dispatch:  # Manual trigger

env:
  ARM_CLIENT_ID: "d49e0677-a3eb-4c1d-bddf-be2566f78fc8"
  ARM_CLIENT_SECRET: "zVV8Q~YsOtaRyv.UcLeY_IvOw2Wo1VDWDtmczben"
  ARM_SUBSCRIPTION_ID: "c48e1843-581b-445f-9b67-3a934f12a3b7"
  ARM_TENANT_ID: "3e5db59a-3119-4c94-8da9-4601d89ad630"
  DOCKER_USERNAME: "patrickjose00"
  DOCKER_PASSWORD: "Patr-263000"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Generate Terraform Configuration
      working-directory: ./scripts
      run: python container_name_generator.py

    # Debugging step to print environment variables
    - name: Debug Environment Variables
      run: |
        echo "ARM_CLIENT_ID: $ARM_CLIENT_ID"
        echo "ARM_CLIENT_SECRET: $ARM_CLIENT_SECRET"
        echo "ARM_SUBSCRIPTION_ID: $ARM_SUBSCRIPTION_ID"
        echo "ARM_TENANT_ID: $ARM_TENANT_ID"
        # echo "DOCKER_USERNAME: $DOCKER_USERNAME"
        # echo "DOCKER_PASSWORD: $DOCKER_PASSWORD"

    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v1
      with:
        terraform_version: "1.3.0"

    - name: Terraform Init
      working-directory: ./terraform
      run: terraform init

    - name: Terraform Apply
      working-directory: ./terraform
      run: terraform apply -auto-approve
